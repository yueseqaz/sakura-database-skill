interface:
  display_name: "Database Agent"
  short_description: "Safe MySQL queries and controlled writes"
  default_prompt: "Use $database-agent to inspect a MySQL schema, build a minimal query or mutation plan, preview its impact, and execute it safely with explicit approval."
