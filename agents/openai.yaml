interface:
  display_name: "Database Agent"
  short_description: "Safe database access for AI agents"
  default_prompt: "Use $database-agent to inspect a database schema, build a minimal query plan, assess its cost, and execute it safely."
