interface:
  display_name: "Database Agent"
  short_description: "Safely discover and query SQL databases"
  default_prompt: "Use $database-agent to inspect this database schema and run a safe read-only query."
