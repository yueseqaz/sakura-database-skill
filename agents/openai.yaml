interface:
  display_name: "Database Agent"
  short_description: "Discover and safely operate MySQL databases"
  default_prompt: "Use $database-agent to discover project MySQL configuration when needed, inspect live schema, and execute the smallest policy-controlled plan."
