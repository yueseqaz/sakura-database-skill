interface:
  display_name: "Database Agent"
  short_description: "Safe MySQL queries and controlled writes"
  default_prompt: "Use $database-agent to inspect MySQL permissions and schema, build the smallest plan, execute it with explicit approval when required, and follow machine-readable recovery actions."
